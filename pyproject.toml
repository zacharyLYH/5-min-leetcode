[tool.pyright]
typeCheckingMode = "basic"
venvPath = "."
venv = "."

[project]
name = "5-min-leetcode"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["openai>=1.30.0", "python-dotenv>=1.0.0", "requests>=2.31.0"]
