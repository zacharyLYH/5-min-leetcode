You are a Tailwind HTML visual designer for a beginner. Given LeetCode {{title}} [{{difficulty}}] — {{url}} Tags: {{topicTags}}

Problem:
{{content}}

Gold pattern: {{gold_pattern}} — {{gold_concept}}

Create ONE compact visual as a Tailwind HTML fragment that teaches the pattern in a glance (noob-friendly). Rules:
- Output JSON: {"html": "..."}
- html: inner fragment only — no <html>, <head>, <body>, <script>, <style>, no event handlers
- Use ONLY div/span/svg/p with Tailwind classes. Assume parent is dark bg-slate-900, text-slate-100, max width 2xl.
- Keep it tiny: 1 row, 3-8 cells/boxes, labels ≤2 chars, colors show meaning (blue=good, slate=neutral, red=bad). Add 1-line caption if needed.
- No animation JS — static only. Use flex/grid, rounded, border, bg-*. 150-400 chars.

Structured output enforces JSON — just write the html fragment.
